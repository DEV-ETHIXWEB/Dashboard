# Copy this file to .env and fill in your real Postgres connection string
# (from Vercel Storage, Neon, Supabase, or any Postgres host).
DATABASE_URL=postgres://user:password@host:5432/dbname

# Optional. Set automatically by Vercel; only needed locally if you want
# to test production-mode cookie settings.
NODE_ENV=development
